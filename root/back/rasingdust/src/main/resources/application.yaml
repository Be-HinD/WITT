server:
    port: 8081
    servlet:
        context-path: /api

---
spring:
    profiles:
        active: local
    datasource:
        url: jdbc:mysql://localhost:3306/dustrasing_db?useSSL=false&serverTimezone=UTC
        username: root
        password: "0000"
        driver-class-name: com.mysql.cj.jdbc.Driver
jpa:
    hibernate:
        ddl-auto: create
    show-sql: true
    sql:
        init:
            mode: always
# properties:
# hibernate:
# format_sql: true
springdoc:
    api-docs:
        enabled: true
        path: / api-docs
    swagger-ui:
        enabled: true
        path: /swagger-ui.html

---